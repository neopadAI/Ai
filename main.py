w = ''
def model(model_name):
  if model_name == 'nnc-- nepsi 1.0 - f':
    import nepsi_1__.__init__ as n
    if w == 'infinite':
      while True:
        n.request(input('Enter your question: '))
    else:
      n.request(input('Enter your question: '))
